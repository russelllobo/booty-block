# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# OTA updates

When asked to run an OTA update in this project, publish to the `production` EAS channel by default.
